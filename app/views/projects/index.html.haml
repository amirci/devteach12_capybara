%h1 Projects

%table#projects
  %thead
    %tr
      %th Name
      %th Description
      %th Active?
      %th Actions

  %tbody   
    - @projects.each do |p|
      %tr
        %td= p.name
        %td= p.description
        %td= p.active?
        %td= link_to "Edit", edit_project_path(p)
